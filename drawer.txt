import 'package:flutter/material.dart';
import 'inicio.dart';
import 'main.dart';
import 'Primera.dart';
import 'Segunda.dart';
import 'Pract4.dart';
import 'JuegoRPS.dart';

class MainDrawer extends StatelessWidget {
  const MainDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: ListView(
        padding: EdgeInsets.zero,
        children: [
          const DrawerHeader(
            decoration: BoxDecoration(
              gradient: LinearGradient(
                colors: [Colors.deepPurple, Colors.indigo],
                begin: Alignment.topLeft,
                end: Alignment.bottomRight,
              ),
            ),
            child: Center(
              child: Text(
                "Menú de Prácticas",
                style: TextStyle(
                  fontSize: 22,
                  color: Colors.white,
                  fontWeight: FontWeight.bold,
                ),
              ),
            ),
          ),
          ListTile(
            leading: const Icon(Icons.home),
            title: const Text("Inicio"),
            onTap: () {
              Navigator.pushReplacement(
                context,
                MaterialPageRoute(builder: (context) => const Inicio()),
              );
            },
          ),
          ListTile(
            leading: const Icon(Icons.filter_1),
            title: const Text("Práctica 1"),
            onTap: () {
              Navigator.pushReplacement(
                context,
                MaterialPageRoute(builder: (context) => const Primera()),
              );
            },
          ),
          ListTile(
            leading: const Icon(Icons.filter_2),
            title: const Text("Práctica 2"),
            onTap: () {
              Navigator.pushReplacement(
                context,
                MaterialPageRoute(builder: (context) => const Prac2()),
              );
            },
          ),
          ListTile(
            leading: const Icon(Icons.filter_4),
            title: const Text("Práctica 4 (Registro)"),
            onTap: () {
              Navigator.pushReplacement(
                context,
                MaterialPageRoute(builder: (context) => const Prac4()),
              );
            },
          ),
          ListTile(
            leading: const Icon(Icons.videogame_asset),
            title: const Text("Juego: Piedra, Papel o Tijera"),
            onTap: () {
              Navigator.pushReplacement(
                context,
                MaterialPageRoute(builder: (context) => const Juego()),
              );
            },
          ),
        ],
      ),
    );
  }
}
